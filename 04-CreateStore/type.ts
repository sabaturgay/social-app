export type User = {
  id: string;
  username: string;
  email: string;
  image: string;
}

export type PostItemData = {
  id: string;
  title: string;
  description: string;
  hashtag: string;
  image: string;
}